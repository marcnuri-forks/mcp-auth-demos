apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubernetes-mcp-server-admin
  labels:
    app.kubernetes.io/name: kubernetes-mcp-server
    app.kubernetes.io/version: v0.0.51
rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]